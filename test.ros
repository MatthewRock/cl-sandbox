#!/bin/sh
#|-*- mode:lisp -*-|#
#| <Put a one-line description here>
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  ;;#+quicklisp (ql:quickload '() :silent t)
  )

(defpackage :ros.script.test.3696748072
  (:use :cl))
(in-package :ros.script.test.3696748072)

(defun main (&rest argv)
  (declare (ignorable argv))
  (and
   (push #P"/home/malice/Programming/Lisp/sandbox/" asdf:*central-registry*)
   (asdf:load-system :cl-sandbox)
   (asdf:test-system 'cl-sandbox)))
;;; vim: set ft=lisp lisp:
