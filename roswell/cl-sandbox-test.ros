#!/bin/sh
#|-*- mode:lisp -*-|#
#| Common Lisp sandbox package for testing out stuff.
exec ros -Q -- $0 "$@"
|#

(defun main (&rest argv)
  (ql:quickload '(:cl-sandbox/tests) :silent t)
  (cl-sandbox/tests:run-tests))
;;; vim: set ft=lisp lisp:
